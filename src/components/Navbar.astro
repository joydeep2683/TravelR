---
const links = [
  { text: "Trips", href: "/TravelR/#trips" },
  { text: "About", href: "/TravelR/#about" },
  { text: "Contact", href: "/TravelR/contact" },
];
---

<nav class="sticky top-0 z-50 w-full backdrop-blur-md bg-white/70 border-b border-white/20 shadow-sm">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <div class="flex-shrink-0">
        <a href="/TravelR/" class="text-xl font-bold tracking-tighter text-stone-800">
          Travel<span class="text-emerald-600">R</span>
        </a>
      </div>
      <div class="hidden sm:block">
        <div class="flex space-x-8">
          {links.map((link) => (
            <a
              href={link.href}
              class="text-stone-600 hover:text-emerald-600 px-3 py-2 rounded-md text-sm font-medium transition-colors"
            >
              {link.text}
            </a>
          ))}
        </div>
      </div>
    </div>
  </div>
</nav>
